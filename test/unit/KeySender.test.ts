// @ts-nocheck
import { describe, test } from '@jest/globals';

describe('KeySender (REMOVED)', () => {
  test('KeySender functionality has been removed from the codebase', () => {
    // KeySender.ts and all key-pressing functionality has been removed
    // The codebase now focuses on reliable terminal read/write operations:
    // - write_to_terminal: Execute commands with optional output capture
    // - read_terminal_output: Read and search terminal output
    // - focus_terminal: Focus the terminal window
    expect(true).toBe(true);
  });
});